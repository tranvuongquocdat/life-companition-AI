# Required for Telegram bot
TELEGRAM_BOT_TOKEN=your_bot_token_from_botfather
TELEGRAM_CHAT_ID=your_chat_id

# Vault path (absolute path to your Obsidian vault)
VAULT_PATH=/path/to/your-vault

# AI Provider (at least one required)
# Option 1: Claude Code OAuth (auto-refresh — recommended if you have Claude Code installed)
# Docker auto-mounts ~/.claude/.credentials.json. No env var needed.
# Option 2: Manual access token (expires ~24h, needs manual refresh)
CLAUDE_ACCESS_TOKEN=
# Option 3: API key (never expires — requires console.anthropic.com access)
CLAUDE_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
GROQ_API_KEY=

# Web Search (optional — free 2,000 queries/month from brave.com/search/api)
BRAVE_SEARCH_API_KEY=

# Optional
DEFAULT_MODEL=claude-sonnet-4-20250514
PORT=3456
MORNING_HOUR=7
EVENING_HOUR=21
TZ=Asia/Ho_Chi_Minh
LANGUAGE=vi
CHAT_MODE=quick
